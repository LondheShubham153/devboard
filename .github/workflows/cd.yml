#Goal : To deploy the appplicayion devboard
name: CD

on:
    workflow_run:
        workflows: ["CI"]
        branches: [advanced]
        
jobs:
    deploy:
        runs-on: self-hosted
        steps:
            - name: checkout
                uses: actions/checkout@v3